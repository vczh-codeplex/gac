@	D2D设备失效
	document里对着一张图片的后一个字符backspace会连图片也一起删掉
	TAB
	不同DPI用不同图片
	Editor.AutoComplete.Grammar 上下选中候选项崩溃，最小化恢复后不能渲染
@	Persistant.LoadFromXml.Controls DataWindow没有内容
@	GacGen.exe处理Persistant.LoadFromXml.ViewModel的XML资源文件失败
@	GacGen.exe处理GacStudio之后，NewFileWindow的TreeView起不到筛选作用